# cli

Command-line client for interacting with a **Minder** instance's API (auth, plugins, RAG, models, bundles).

Status: **planned / proposed** (not yet tracked by an issue). Open-source companion to the Minder platform, alongside `plugin-sdk`.